- [x] Review `exercises/clippy/clippy1.rs` to identify the Clippy lint.
- [x] Update the code so it passes Clippy without changing behavior.
- [ ] Run `cargo clippy` for this exercise to verify the fix。直接运行 `cargo clippy --bin clippy1` 报错：默认 crate 并无 `clippy1` 可执行；需改用 `rustlings clippy clippy1` 或对应命令确认。
- [ ] Report the changes and suggest any next steps to the user.
